import 'package:flutter/material.dart';

class ProjectActionDetailedWidget extends StatelessWidget {
  const ProjectActionDetailedWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
