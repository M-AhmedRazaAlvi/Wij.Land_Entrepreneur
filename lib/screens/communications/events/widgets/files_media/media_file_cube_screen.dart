

import 'package:flutter/material.dart';

class MediaFileCubeScreen extends StatelessWidget {
  const MediaFileCubeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('no  media file here'),
    );
  }
}
